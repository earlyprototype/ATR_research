# REGISTER INTEGRITY AUDIT — _STAGE2_JSPACE experiment/hypothesis register

Audited at local checkout `/home/user/ATR_research` (branch `claude/repo-review-alignment-knzmrj` = `origin/main` tip `eb86404`), plus `origin/claude/issue-6-akvuxp` (`371e754`, pre-merge `498e7ca`), git history, GitHub issue #52, PRs #9/#10.

## SUMMARY

The register is unusually disciplined at the single-spec level — every spec carries a frozen mechanical outcome table, deviations are recorded flat, post-run addenda are labelled — but it fails at the *system* level: concurrent agent sessions cannot see each other's branches, so the register has now produced **two documented cases of the same experiment being pre-registered twice with different frozen parameters and run twice** (EXP_010c-3, flagged in issue #52; the EXP_010c permutation control, **not flagged anywhere** — found in this audit), plus a **three-way hypothesis-ID collision** (H11, H11a, H12 each bound to two different statements with recorded verdicts under both). Issue #52's factual claims all verify against git. In the permutation-control case the earlier executed run's results section was **silently deleted at a merge** (PR #9, commit `359c622`), violating the repo's own "appended to, never rewritten / superseded stays visible" conventions. A merge resolving issue #52's numbering questions was executed by an agent on 2026-07-31 (`371e754`, on the PR #10 branch, citing the discussion #37 ruling) while issue #52 — which states "PR #10 stays unmergeable until a human rules on this" — is still open with no visible human ratification.

## KEY FACTS

**(1) Experiment register table** (all paths relative to `_STAGE2_JSPACE/`):

| Exp ID | Spec file | Purpose | Status field (verbatim, abridged) / outcome |
|---|---|---|---|
| EXP_010b | `EXP_010b_SPEC.md` | H5-coarse window grid on GPT-2 Small, arms SB,S1–S5 | "Status: committed BEFORE any run (registered). Issue: #16." Run 2026-07-26; gate PASS 25/25; **H5-coarse SUPPORTED** (`experiments/exp_010b_small/RESULTS_EXP010B.md:131`) |
| EXP_010c | `EXP_010c_SPEC.md` | Medium window grid: is the `D` collapse cut-dependent (H9/H9a) | "Status: PRE-REGISTERED — recorded before any run. Created: 2026-07-23." Run; **H9 SUPPORTED, H9a SUPPORTED w/ caveat** (`RESULTS_EXP010C.md:141,145`) |
| EXP_010c-2 | `EXP_010c2_SPEC.md` | Band boundary scan (H10/H10a/H10b) | "PRE-REGISTERED — recorded before any run. Queued behind the EXP_010c registered full run." Run; **H10 SUPPORTED, H10a REFUTED in strong form, H10b SUPPORTED** (`RESULTS_EXP010C.md:185-208`) |
| EXP_010c-3 | `EXP_010c3_SPEC.md` | In-fill grid, 9 arms around word cells (H12/H12a/H13) | "PRE-REGISTERED — recorded before any run of these arms. Created: 2026-07-24." Run; **H12 REFUTED, H12a SUPPORTED (onset), H13 SUPPORTED, edge j=21** (`RESULTS_EXP010C.md:426-446`). **Registered a second time** on `claude/issue-6-akvuxp` under H11/H11a/H11b — issue #52 |
| EXP_010c-3b | `EXP_010c3b_SPEC.md` | 5 follow-up stress checks on the in-fill | "PRE-REGISTERED — recorded before any of these results exist. … Issue: #21." Run; H12 refutation survives 2b; seed shown to be a no-op |
| EXP_010c-PERM | `EXP_010c_PERM_SPEC.md` | Anisotropy-corrected permutation test on terminal sets | "PRE-REGISTERED — recorded and committed before any statistic is computed. Issue: …#7." Executed (seed 20260725); mixed outcome, A4 passes at z≈+4.7 |
| *(none)* | `PERM_TEST_EXP010c_SPEC.md` | **Duplicate earlier registration of the same test** (issue #7) | "PRE-REGISTERED — recorded before any computation." Executed 2026-07-24 on PR #9 (seed 2026); results **discarded at merge**; spec orphaned on main with no supersession marker |
| EXP_010c-ROBUST | `EXP_010c_ROBUST_SPEC.md` | Seed/subset robustness of A0/A4/O8 | "PRE-REGISTERED — committed before any run." Issue #11. Run; same-basin-structure ("stable") |
| EXP_010c-VARIANTS | `EXP_010c_VARIANTS_SPEC.md` | Hook-point (Control A) + energy-norm (Control B) | "PRE-REGISTERED — committed before any run." Issues #13/#14. Run |
| EXP_010d | `EXP_010d_SPEC.md` | Capstone: does windowed Medium recreate Small's basin partition (H11/H11a) | "PRE-REGISTERED — recorded before the Small run." **H11 REFUTED (robustly); H11a REFUTED for A4** (`RESULTS_EXP010D.md:80,87`). Reached main only via rescue commit `c4bf9c0` (2026-07-27) after living on stranded PR #5 |
| EXP_012-PYTHIA | `EXP_012_PYTHIA_SPEC.md` | Placebo window grid on Pythia-410m; absorbs the never-run EXP_010a (H8) | "PRE-REGISTERED — recorded before any registered run. Issue: #12." Run; **H8 SUPPORTED** under seed_j convention (`RESULTS_EXP012_PYTHIA.md:110-113`) |
| EXP_012m / EXP_011m / EXP_013m | `RUNBOOK_JLENS_MEDIUM.md` §1 | Medium J-lens track: band census / terminal projection / re-decode | "Status: PLANNED — priors in §3 are deliberately left open until EXP_010c reports." EXP_012m run (issue #15): **NO COHERENT BAND** (`RESULTS_JLENS_MEDIUM.md:197-198`); EXP_011m/EXP_013m "NOT run here" (`RESULTS_JLENS_MEDIUM.md:6-9`) |
| EXP_010c-4 | `EXP_010c4_SPEC.md` — **branch only, not on main** | Full 277-cell window census | "PRE-REGISTERED — recorded and committed before any census run." Hypothesis H12→renumbered H14 at `371e754`; **SUPPORTED (15 of 50 eligible cells)** — unmerged (PR #10) |

**(2) Verification of issue #52's core claims** — all checked against git, all correct:
- Two `EXP_010c3_SPEC.md` files: branch `aea658f` 2026-07-24 16:11:54 (H11/H11a/H11b) vs main `3ee3126` 19:25:24 (H12/H12a/H13); each committed before its own session's first arm (16:30:50 / 19:56:25). Add/add conflict; merge base `4e45d4b`.
- Both sessions ran the same 9 arms; artifacts bit-identical per issue #52 §5 (JSON leaves re-verified there; I verified the concordant verdict rows on both refs).
- Main's spec contains the explicit dodge: "Numbering note: H11 is used by the EXP_010d line (PR #5…); this spec continues at **H12** to avoid collision" (`EXP_010c3_SPEC.md:44-45`). The branch could not see EXP_010d (`git ls-tree` confirms no 010d files there).
- Ruling exists: discussion #37 (2026-07-26), quoted in issue #52's comment from the `board-state` mirror: "H11 belongs to the EXP_010d capstone line… If PR #10 lands in any form, its spec renumbers to match PR #33's H12-series before merge." Cited on main at `RESULTS_EXP010D.md:147-148` and `sessions/SESSION_2026-07-27_MEDIUM_TENSOR_SYNTHESIS.md:76-77`.
- On 2026-07-31 10:07 UTC, merge `371e754` on the PR #10 branch implemented main-wins + census H12→H14. Main untouched; PR #10 open, `mergeable_state: clean`; issue #52 still open.

**(3) New finding of this audit — the permutation control was ALSO double-registered and double-run:**
- `PERM_TEST_EXP010c_SPEC.md` committed `97aeb20` 2026-07-24 19:24:26 (branch `claude/issue-7-mht248`, PR #9); its run committed `e734ba0` 19:30:00 with a full results section "## 2026-07-24 — Anisotropy-corrected permutation test (planned control 1)" (seed 2026, α=0.05/8, token-ID-decile frequency bands; A4 4.6σ pass, all else null).
- `EXP_010c_PERM_SPEC.md` committed `0ca5829` 2026-07-25 11:26:00 (different session, PR #19 line), run `798e2fc` 11:28:14 (seed 20260725, α=0.05/9, merges.txt rank//5000 banding; A4 z=+4.69 pass, all else null).
- Merge `359c622` (in PR #9's branch, conflicts on `RESULTS_EXP010C.md`, `permutation_results.json`, `permutation_test.py`) resolved by taking main's versions; PR #9's final merge to main (`5c4cb24`) added **only the orphaned spec**. Main's `RESULTS_EXP010C.md` today contains **zero trace** of the 07-24 run (grep for "PERM_TEST\|seed 2026" is empty). The PR #9 agent recommended "close this PR"; the owner merged it anyway (merged 2026-07-25 15:15:48Z).

**(4) Requested greps** (`grep -rn "H1[0-9]"` / `H[0-9]\b` over `_STAGE2_JSPACE`): 142 H1x matches across 41 files; token tally: H8×27, H5×22, H9×15, H12×14, H11×14, H9a/H13/H10a/H10b×10, H12a×7, H6/H7/H10×5, H11a×3, H0/H4×1 each (only in `STAGE2_OUTLINE.md:39` "continuing Stage 1's H0–H4 numbering"). H1/H2/H3 appear nowhere. **Caution:** all "H12"/"H2O" hits inside `results_*.json` are prompt text (`C6H12O6`, `H2O`) — no result JSON embeds an H-label.

## CLAIMS AND HYPOTHESES

Ground-truth table. "Def" quotes are verbatim (abridged with …).

| ID | Where defined | Statement (verbatim, short) | Status per source | Artifacts support? |
|---|---|---|---|---|
| H0–H4 | Stage 1 (other repo); referenced only at `STAGE2_OUTLINE.md:39` | — | — | H1–H3 are unallocated gaps in this repo |
| H5 | `STAGE2_PLAN.md:21`; variant wording `STAGE2_OUTLINE.md` ("H5 (band-dependence)…"); quoted in `EXP_010b_SPEC.md:20-22` | "Layer-window loops (inject i, extract j) produce qualitatively different landscapes *within* the putative workspace band vs *across* band boundaries" | **H5-coarse SUPPORTED** (`RESULTS_EXP010B.md:131`) | Yes — 150/150 arm artifacts committed per arm; gate PASS first |
| H5-coarse | `EXP_010b_SPEC.md:28-34` (derived mechanical rule) | "SUPPORTED if at least one sub-stack window differs from the 0→11 baseline on any of the pre-stated comparison fields…" | SUPPORTED | Yes |
| H6 | `STAGE2_PLAN.md:22`; `STAGE2_OUTLINE.md` ("workspace capture") | Small's 5 basin tensors "project significantly more onto the J-space than the 18 null-model basins" | Untested (EXP_011 pending) | n/a |
| H7 | `STAGE2_PLAN.md:23`; `STAGE2_OUTLINE.md` ("small-model workspace") | "A coherent J-lens band structure exists at 124M–410M scale, and its presence/shape predicts each model's ATR regime" | Untested; EXP_012m's NO-COHERENT-BAND is input | n/a |
| H8 | `STAGE2_PLAN.md:24`; `STAGE2_OUTLINE.md` | "Pythia-410m's fragmentation is depth-dependent: looping layers 0–11 (vs native 0–23) changes convergence behaviour" | **SUPPORTED** "under the registered seed_j energy convention" (`RESULTS_EXP012_PYTHIA.md:110-113`) | Yes; the energy-convention caveat is honestly load-bearing (i=0 arms inject at 63×/403× natural norm) |
| H9 / H9a | `EXP_010c_SPEC.md:29-38` only | H9: "GPT-2 Medium's single-basin `D` collapse is cut-dependent…"; H9a: "the window most different from baseline sits inside the paper-mapped workspace band (layers ≈10–21…)" | **SUPPORTED / SUPPORTED** ("on the registered wording, with a recorded measure caveat" — 'most different' had no single pre-registered metric) (`RESULTS_EXP010C.md:141-150`) | Yes; H9a's caveat is candid about an under-specified criterion |
| H10 / H10a / H10b | `EXP_010c2_SPEC.md:30-40` only | H10: "landscape character … changes non-uniformly as a window edge slides"; H10a: "re-including the last two layers … collapses the band window's landscape back"; H10b: "extending the window to include the sensory front (0→21) degrades richness" | **SUPPORTED / REFUTED in its strong form / SUPPORTED** (`RESULTS_EXP010C.md:185-208`) | Yes |
| **H11** | **Def A** `EXP_010d_SPEC.md:33` (claimed `baf3398` 07-24 04:37): "H11 (geometry recreation): … the basin *partition* induced by the Medium workspace-band loop (A4, 10→21) agrees with the basin partition induced by the native GPT-2 Small loop (0→11) significantly above chance, and more than the Medium full-stack baseline (A0…) does" · **Def B** `498e7ca:_STAGE2_JSPACE/EXP_010c3_SPEC.md:39` (07-24 16:11): "H11 (zone contiguity): the whole-word injection zone is contiguous: 9→21 produces whole-word, prompt-dependent terminals…" | two unrelated statements | A: **REFUTED (robustly)** (`RESULTS_EXP010D.md:80`) · B: **REFUTED** (branch `RESULTS_EXP010C.md:418`) | Both mechanically applied to committed data; ID collision confirmed. Ruling: H11 → EXP_010d (discussion #37) |
| **H11a** | **Def A** `EXP_010d_SPEC.md:37`: "H11a (basin count): A4's effective basin count is closer to Small's than A0's is" · **Def B** branch spec: "H11a (edge localisation at one-layer resolution): each edge of the zone completes its transition within one layer…" | two unrelated statements, **opposite verdicts** | A: **REFUTED for A4** (`RESULTS_EXP010D.md:87`) · B: **SUPPORTED** (branch `RESULTS_EXP010C.md:422`) | Collision confirmed. Note: the #37 ruling's "H11a … vacated (existed only in PR #10's numbering)" is factually wrong — Def A predates and carries a verdict (issue #52 comment §0 records this) |
| H11b | branch only, `498e7ca:…EXP_010c3_SPEC.md:52`: "for i ∈ {8, 10}, the whole-word class persists at every extraction depth j ∈ {15, 17, 19, 21}" | inverse-polarity twin of main's H13 | **NOT SUPPORTED; refutation row fires at (10,19)** (branch `RESULTS_EXP010C.md:437`) | Same bitwise data as H13; "NOT SUPPORTED" (not "REFUTED") is correct per its own asymmetric outcome table; its table fails to cover 3 of 9 cells |
| **H12** | **Def A** `EXP_010c3_SPEC.md:47` (main, 07-24 19:25): "H12 (injection-zone continuity): the whole-word prompt-dependent zone … is **continuous** across i ∈ {8,9,10}" · **Def B** branch `EXP_010c4_SPEC.md` (07-24 20:06): "H12 (aliasing materiality): the census contains at least one cell whose arm class … differs from the arm class of **every** already-measured cell adjacent to it" | two unrelated statements, **opposite verdicts** | A: **REFUTED** (`RESULTS_EXP010C.md:426`) · B: **SUPPORTED (15 of 50 eligible)** (branch `RESULTS_EXP010C.md:1362`) | Collision confirmed; Def B renumbered **H14** at `371e754` (2026-07-31), label-only change |
| H12a | `EXP_010c3_SPEC.md:53` (main only) | "the flanking odd layers 5→21, 7→21 and 11→21 fall on the *off-band* side of the transition…" | **SUPPORTED on the onset side, with a recorded sharpening** (sub-prediction "7→21 whole-word but 5→21 not" NOT borne out — recorded flat) (`RESULTS_EXP010C.md:431`) | Yes |
| H13 | `EXP_010c3_SPEC.md:59` (main only) | "at fixed injection (8 or 10), landscape character … varies **non-uniformly** as the extract layer j descends 21→19→17→15 — there is an identifiable extraction depth below which the whole-word, via-tail-robust character is lost" | **SUPPORTED; edge at j=21, sharp** (`RESULTS_EXP010C.md:440`) | Yes on the 9 in-fill cells; **tension**: the branch census records j=21's eleven census arms averaging 6.6/25 via-tail agreement vs the two pre-census cells (17/25, 23/25) the "sharp edge" rests on — flagged in issue #52 comment N3, unadjudicated |
| H14 | branch post-merge only (`371e754:EXP_010c4_SPEC.md:43`) | "H14 (aliasing materiality)" — renumbered from census H12, criterion unaltered; §6 amendment (recorded ~1 arm in) restricted it to cells with ≥1 measured neighbour to avoid vacuous support | SUPPORTED (15/50) | Lives only on unmerged PR #10 |
| Q-D | `STAGE2_PLAN.md:25` | "(Question, not hypothesis) What is the `Divine` object…" | open | — |

**Issue #52 adjudication ground truth:** the issue's three-way-collision claim is **correct and, if anything, still understated** — this audit adds a fourth same-class event (the permutation-control double registration, §3 of KEY FACTS above), which issue #52 does not mention.

## CONFLICTS/CONTRADICTIONS

1. **H11 / H11a / H12 three-way ID collision** — verified verbatim above; H11a and H12 carry *opposite* verdicts under their two meanings.
2. **Discussion #37 ruling contains a factual error**: "H11a … vacated (they existed only in PR #10's numbering)" contradicts `EXP_010d_SPEC.md:37` (H11a defined, verdict recorded). Registry table needs correcting.
3. **`.claude/skills/peer-board/SKILL.md:119-120` "H11 was claimed three separate ways"** is an over-count: two claims (PR #5, PR #10) + one explicit deferral (PR #20's "continues at H12 to avoid collision"). The governance doc all agents load misstates its own founding incident.
4. **Two permutation-test pre-registrations with different frozen parameters** (seed 2026 vs 20260725; α 0.05/8 vs 0.05/9; token-ID-decile vs merges.txt rank//5000 banding; the earlier spec additionally required "effect size > 2σ") both claiming to execute "planned control 1"; main's register shows only one run where two occurred.
5. **`EXP_012` means three things**: STAGE2_PLAN/OUTLINE's Phase-3 cross-model J-lens census (H7, never run); EXP_012-PYTHIA, an ATR placebo grid testing H8 (i.e., the EXP_010a design — `EXP_010b_SPEC.md:183`: "EXP_010a's successor ran as EXP_012"); and EXP_012m, the Medium band census. No register entry flags the reuse.
6. **`RUNBOOK_JLENS_MEDIUM.md` §3 branch table has no row for the actual outcome** — H9+H9a SUPPORTED *and* EXP_012m NO COHERENT BAND; the only row contemplating a missing band is the "H9 refuted" row (issue #52 comment N3; independently verified against the runbook text).
7. **H13 "sharp edge at j=21" vs the branch census's j=21 row average 6.6/25** — unresolved tension between a merged verdict and unmerged data.
8. **Issue #52's filing says "PR #10 stays unmergeable until a human rules"**, yet merge `371e754` (2026-07-31, agent-authored) resolved the conflict on the PR branch citing #37's pre-authorization; issue #52 is still open and no human ratification of the §10 items is visible in the record.
9. Minor: `EXP_010d_SPEC.md:66-67` names its frozen input `terminals_full_A0A4.pt(.bak)`; no file of that name exists on main (actual artifact `output/terminals_full.pt`); the results record does not note the mismatch.

## RED FLAGS

1. **Silent destruction of an executed control's record.** PR #9's 2026-07-24 permutation run (spec `PERM_TEST_EXP010c_SPEC.md`, results committed `e734ba0`) was erased from `RESULTS_EXP010C.md` at merge `359c622`; main retains the orphaned spec labelled "PRE-REGISTERED" with no supersession marker and no pointer to its run. This directly violates the register's own convention ("Existing sections appended to, never rewritten" — `EXP_010c3b_SPEC.md` Conventions). The two runs were concordant, so the hidden event is actually *favorable* replication evidence — hidden anyway.
2. **The register cannot guarantee a unique binding pre-registration.** Two independent double-registration incidents (EXP_010c-3; permutation control) in a 24-hour window, both caused by branch-isolated sessions. The Identifier-registry board thread postdates all four specs and is invisible to branch-only sessions; no in-repo file-based guard exists.
3. **Same IDs, opposite recorded verdicts on different refs** (H11a SUPPORTED vs REFUTED-for-A4; H12 SUPPORTED vs REFUTED) — anyone citing "H12" or "H11a" without a ref is ambiguous until PR #10 merges.
4. **Verdict-bearing material stranded off-register**: EXP_010d's H11 verdict lived only on doomed PR #5 until the 07-27 rescue (`SESSION_2026-07-27…:70-79` calls this out); the 300-cell census + H14 verdict still live only on unmerged PR #10.
5. **Adjudication provenance is murky**: every issue, PR, comment and "ruling" is authored by the single GitHub account `earlyprototype`, with agent-generated text posted under it; the #37 ruling asserts operator delegation that "no artifact … corroborates or contradicts" (issue #52 comment, "Could not resolve" §2). Whether the 2026-07-31 merge resolution had human sign-off cannot be established from the record.
6. **Main committed no run log for the in-fill tier** while every other tier has one (24 logs; issue #52 §6) — a record-completeness gap on the very sweep that was double-run.
7. Minor audit trap: H-ID greps false-positive on `C6H12O6`/`H2O` in prompt JSONs; naive grep-based register audits will miscount.

## RECOMMENDATIONS

1. **Human ratification pass on issue #52 §10** (and the comment's D/S/N proposals) recorded *in the issue by the human*, before PR #10 merges — the 371e754 resolution implements the ruling but its authorization trail is agent-only.
2. **Create an in-repo `REGISTER.md`** (every EXP-ID and H-ID → owning spec, one-line statement, status, ref) as the file-based backup to discussion #37; add a CI/merge check that any new `H\d+` or `EXP_\d+` token in a spec appears there. This was proposed in issue #52 §11 and is still unimplemented on main.
3. **Repair the permutation-control record**: mark `PERM_TEST_EXP010c_SPEC.md` SUPERSEDED with a pointer to `EXP_010c_PERM_SPEC.md`, and append a dated note to `RESULTS_EXP010C.md` recording that planned control 1 was independently registered and executed twice with concordant outcomes (commits `97aeb20`/`e734ba0` vs `0ca5829`/`798e2fc`).
4. **Disambiguate EXP_012** (rename or register-note the pythia grid vs the Phase-3 H7 census vs EXP_012m), and register `EXP_010c-4` on main when PR #10 lands. Note for the record: **EXP_014 does not exist anywhere** (main or branch); the only "14" allocation is hypothesis H14 (2026-07-31).
5. **Adjudicate the H13-vs-census j=21 tension** as its own registered item before the J-lens phase consumes "extraction 21 is the only reliable depth" as a prior.
6. **Correct the two governance-doc inaccuracies**: SKILL.md's "claimed three separate ways" and the #37 registry table's "H11a → vacated".
7. Add the cheap merge guard proposed in issue #52 S2: every tier with committed artifacts must have a committed run log.