# AUDIT — Scientific Evidence Strength, ATR_research Stage 2 (J-space)

Auditor basis: all claims below were checked against the local checkout at `/home/user/ATR_research` (tip `eb86404` = main). Where readers disagreed I re-read the primary files, recomputed or spot-checked artifact numbers, verified git timelines, checked GitHub PR #5 via the API, and fetched the upstream `anthropics/jacobian-lens` repo.

## VERDICT

Stage 2 is an unusually honest record whose mechanical verdicts are almost all genuinely supported by committed artifacts — I confirmed the reproduction gates, the census null, the permutation table, the VARIANTS controls, and the H11/H12 refutations against primary files, and the one external dependency the whole J-space arm rests on (`anthropics/jacobian-lens`, companion code to the Transformer Circuits workspace paper) is real. But the programme's *positive* story has hollowed out while its bookkeeping stayed clean: the Medium J-lens gate is MARGINAL (legitimately, per a pre-registered PASS/FAIL/MARGINAL vocabulary), the band census is a mechanical null, no sub-window on Small carries the Stage 1 landscape, window-position effects reproduce on a placebo model, and the flagship `D` collapse is now known to exist only under a layer-0 hook plus ~218x over-natural injection energy — which quietly re-scopes the Stage 1 cross-model headline that motivated Stage 2 in the first place. The workspace framing survives on exactly one unrun experiment class (EXP_011/EXP_011m subspace overlap), the plan-level kill criteria as literally written can never fire, and the register has three genuine integrity defects (five artifact-contradicted numbers in RESULTS_EXP010B.md, a silently double-registered permutation spec, and an H11 pre-registration that is testimonial rather than commit-verifiable). The highest-information next move is not more J-lens work: it is the still-unfiled natural-energy ARI test of the "apparatus mask" hypothesis, which costs nothing and bears on whether the programme's founding observation is a property of the models or of the apparatus.

## DETAILED FINDINGS

Evidence grades for every headline claim (SOLID / SUPPORTED-BUT-FRAGILE / UNVERIFIED / CONTRADICTED):

**1. H5-coarse, Small window grid (EXP_010B) — SOLID (verdict); the descriptive layer of the same file is CONTRADICTED.**
The reproduction gate is exact (25/25 on `(converged, terminal_token)` incl. lock iterations vs the Stage 1 record inside the committed `_DATA` zip; independently recomputed by code-verify, log committed). The verdict follows mechanically from `EXP_010b_SPEC.md` §1's rule. Two caveats leadership must carry: (a) the rule ("≥1 of 5 windows differs on any field") was near-unfalsifiable, so "SUPPORTED" carries almost no information — the informative result is the *null*: no sub-stack window reproduces the Stage 1 semantic landscape, so there is no ATR-side candidate workspace band on Small; (b) the i=0 arms (incl. the baseline itself) inject at 56–175x natural norm, so "the full stack is the only window with the landscape" is energy-convention-conditional (SUPPORTED-BUT-FRAGILE). Separately, I confirmed code-verify's integrity finding: `RESULTS_EXP010B.md:45` claims SB basin sizes (6,5,4,3,3,2,2) and lines 52–54 claim off-diag cosine minima 0.339/0.514/0.984/0.869 (SB/S2/S3/S5); the committed artifact `terminal_characterisation_small010b.json` says [6,5,4,3,3,3,1] and 0.6921/0.4983/0.97/0.441. The means match; five numbers match no committed artifact. Not verdict-bearing, but a real breach of the "committed artifact = authority" discipline.

**2. Word-forming windows A4 (10→21) and O8 (8→21) on Medium — SUPPORTED-BUT-FRAGILE.**
The best positive result in the programme, verified in `RESULTS_EXP010C.md` and its artifacts: A4 = 12 tensor basins, 3 whole-word prompt-dependent terminals, via-tail 23/25, margins 4.20/7.15 — the only arm in the grid with non-trivial j<23 via-tail agreement. Survives: disjoint prompt subset (ROBUST V3: A4 `until`x22; O8 `simultaneously`x18), hook-point convention (HP9 identical to A4 on all 9 fields x 25 records), i-scale energy normalisation (class unchanged at 1.04–1.05x natural), and seed (vacuously — protocol deterministic). Fragile because: it is a single-instrument measurement (logit-lens-at-j + via-tail of the same apparatus); the second instrument sees nothing (census: J agree-final 0.02/0.02/0.02 at layers 8/9/10 — I verified in `census_results.json`; robust to the one-layer convention shift); the stopping-rule stability check at these exact cells — which the project's own 2026-07-25 session note calls the key untested assumption — was proposed and never specced or run (grep confirms it ran only at I7/I9/X1017); n=25 per cell; and O8's terminal set sits *at its null mean* in the permutation control (z=−0.08), so the "temporal-durative family" reading is A4-only.

**3. The 9→21 refutation (H12) — SOLID.**
The pre-registered refutation branch exists verbatim (`EXP_010c3_SPEC.md:126`: "9→21 funnels to punctuation/fragments or a single shared terminal → H12 refuted — 8→21 and 10→21 are separate one-layer islands"). Observed: I9 `oooooooo`x18/`…`x7, whole-word 0/25 (artifact-verified). Survives a fully disjoint prompt subset (whole-word 1/25) and a stopping-rule change at class level (I9's terminal *identity* flips completely, its non-word *class* does not). The islands are islands, not a band — as scoped to the logit-lens readout, which is how it was registered.

**4. The anisotropy-corrected permutation result — SOLID on the A4 rows; SUPPORTED-BUT-FRAGILE as a broader claim; register hygiene hole confirmed.**
A4_direct p=0.00090 (z=+4.69) and A4_tail p=0.00190 pass Bonferroni α*=0.00556; table matches `permutation_results.json` exactly (code-verify recomputed; I verified the results section). The A4 pass is robust to *either* registration's threshold. The hole: two files both marked "PRE-REGISTERED" for the same control (`PERM_TEST_EXP010c_SPEC.md`, 07-24, seed 2026, α=0.00625, token-ID banding, pool-relaxation rule; `EXP_010c_PERM_SPEC.md`, 07-25, seed 20260725, α=0.00556, merges-rank banding, +A1_tail contrast). Only the later was executed; the earlier is referenced by no file (grep: zero matches) and carries no supersession marker. Git timestamps (97aeb20 07-24 19:24 → 0ca5829 07-25 11:26 → results 798e2fc) show the rewrite preceded the run — an honest re-registration, not p-hacking — but as committed, the register permits after-the-fact spec selection, and one threshold-sensitive outcome (A1_tail p=0.00620) sits exactly between the two thresholds. O8, A5 and both pooled sets do not pass.

**5. Hook-point and energy-norm controls (EXP_010c-VARIANTS) — SOLID.**
Spec pre-committed (fa9f9cd) before any run. Verified in the record: HP9 hook equivalence exact; injection at layer 1 removes `D` (1→23 → `name`x22) while 0→21's `,` funnel survives i=1; under natural-energy renorm A0 and A1 have 0/25 convergence and `D` is absent; measured seed_j/natural ratio at A0 is 217.8x. The combined statement — `D` observed only under {layer-0 resid_pre} ∧ {~218x natural energy}, removing either removes it; the A4/O8 landscape unchanged under every variation tested — is exactly what the artifacts say. Consequence not yet propagated: the Stage 1 headline "GPT-2 Medium collapses to one empty token" is CONTRADICTED in its unconditional form — it is a property of the apparatus configuration, not of Medium simpliciter — and the early H9/H10a/H10b verdicts phrased against "the collapse" were never re-graded (a dated annotation is owed). Note the same class of caveat hangs over Small's landscape (SB at 73x natural, natural_i mirror unrun): the entire Stage 1 cross-model contrast is convention-conditional.

**6. H11 refutation (EXP_010d) — refutation SUPPORTED-BUT-FRAGILE (near-solid); companion A0↔Small observation SUPPORTED-BUT-FRAGILE; chain of custody defective.**
Numbers all verified against `basin_comparison_full.json`: A4↔Small ARI 0.048, p=0.244, non-significant at all four thresholds (0.99–0.9995) — the band window does not recreate Small's geometry. The unexpected converse — the all-`D` baseline A0 partitions the prompts like Small, ARI 0.200, p=0.0009, threshold-robust 0.995–0.9995 — is real in the artifacts but carried by 18/25 prompts in one cluster, against a Small reference containing 7 never-converged Divine points, on one subset. My own custody finding (checked via the GitHub API): PR #5 is a **single commit** (baf33989) containing the "pre-registered" spec, the harness, AND the results together — unique in the family; every other spec has a commit-verifiable spec-before-results sequence. H11's pre-registration is therefore testimonial. Mitigation: the refutation runs *against* the programme's own hypothesis, so gaming is implausible. The 07-27 rescue also ported known-stale caveats verbatim ("single seed" after seed was shown vacuous; no stopping-rule caveat on the terminal-tensor partitions).

**7. Medium J-lens gate MARGINAL — SOLID as recorded.**
The split is genuinely in the data (gate table matches `gate_results.json`; content positions: J-lens wins 6/6 with real semantic-neighbourhood transport; prediction positions: prompt-independent junk stems ≤L12, task-relevant only from ~L15). First run, no retries, per the fail-is-evidence clause. On the disputed vocabulary point: MARGINAL **is** pre-registered — see Disputed Points. One fair residual criticism (results-other): against STAGE2_PLAN P0-3's wording ("multi-step prompts... more interpretable"), the multi-hop-specific half failed outright (`Italy` never surfaces anywhere), so MARGINAL sits at the generous end of an honest range. The instrument itself is now verified real: I fetched `github.com/anthropics/jacobian-lens` — Apache-2.0, single-commit reference implementation, companion to "Verbalizable Representations Form a Global Workspace in Language Models" (Transformer Circuits). foundations' largest red flag is retired.

**8. EXP_012m no-coherent-band — SOLID, as scoped.**
Zero lens-dominant layers under the three-criterion rule; no layer passes even criterion (i); recomputed from raw per-prompt ranks by code-verify and aggregate-spot-checked by me (layers 8/9/10/21/22 match). Multi-hop null on both lenses (≤1% LL, 0% J). The BOS scoring bug was disclosed, rerun once, verdict invariant. My timeline check settles the rule-timing dispute in the project's favour (see Disputed Points). Scope limit that stands: the census scores position −1 only, so "no coherent band" is established for prediction-position dominance; a content-position census (same pipeline, position −2) was never run and is cheap.

**9. Pythia placebo/depth control (EXP_012-PYTHIA) — H8 SUPPORTED-BUT-FRAGILE; placebo finding SOLID.**
H8's verdict is mechanical on four pre-registered axes (P-A1: 25/25 converged, 1 basin, cos 1.000 vs P-A0: 13/25, 15 basins, cos 0.486) — but the two arms inject at 63x vs 403x natural norm, so depth and loudness co-vary under the registered convention, exactly as the register itself says; and the "consolidates to ` bast`" reading would overclaim (margin 0.02, via-tail 0/25 — only the tensor-level consolidation is robust). The placebo result is the strategically decisive one and is solid: window-position effects are PRESENT on a model with no known consolidation (conv 13/25–25/25, basins 1–25 across arms; 10→21 even reproduces "exactly 3 word-like terminals" as a feature coincidence). Window effects are therefore not, by themselves, workspace signatures — this strips the Medium map of its specificity as band evidence.

**10. "Medium tensor finding" synthesis (SESSION_2026-07-27) — UNVERIFIED (by design, and correctly labelled).**
The three inputs (A0↔Small tensor ARI; `D` energy-bound; `D` slot-bound) are each individually verified in merged records. The synthesis — "the `D` collapse may be an apparatus mask, not the phenomenon" — is explicitly fenced as hypothesis-not-finding, with alternatives that would kill it stated. But all three named arbiters are outstanding: EXP_013m (#46) not run and pre-declared ambiguous-on-null; EXP_011m (#45) not run; and the natural_i ARI test — the only one that directly discriminates the mask hypothesis — is *still not filed anywhere* (grep confirms it exists only in the session note). The hypothesis is unfalsified because it is untested.

**Cross-cutting code findings (confirmed):** the artifact-overwrite guard in `run_exp010c.py` still omits `--arms` from the variant list (`--tier full --arms A5` would silently overwrite the registered `results_full.json`), and a `--tag` value colliding with a tier name is unchecked. The engine passes raw prompt strings to `run_with_cache`, so the deviation note "the ATR engine does not prepend BOS" (`RESULTS_JLENS_MEDIUM.md` §5.4) is probably backwards (transformer_lens defaults `prepend_bos=True` for GPT-2) — a live convention seam sitting directly in the path of EXP_011m/EXP_013m. Phase 0 for Small (RESULTS_PHASE0.md, the Small lens fit and gate) is absent from this repo entirely, and EXP-D (Q-D) has produced no artifacts anywhere in the checkout.

## DISPUTED POINTS RESOLVED

1. **Is MARGINAL a pre-registered verdict or a post-hoc invention?** foundations right, results-other wrong. `RUNBOOK_PHASE0.md:86` mandates "**PASS / FAIL / MARGINAL** verdict goes in RESULTS_PHASE0.md," and the Medium gate is explicitly "Validation gate (P0-3, Medium edition)" (`RUNBOOK_JLENS_MEDIUM.md` §2.2), inheriting that vocabulary. results-other's residual point survives in weakened form: §2.2's *consequence clause* ("No Stage 2 conclusion uses the Medium lens before this gate passes") is binary, but running EXP_012m after MARGINAL is consistent with the runbook's own fail-is-evidence branch, and the lens-consuming experiments (011m/013m) have not run — no violation occurred.

2. **Was the census band rule fixed after the gate result was known?** results-other wrong on the facts. Commit `0f478a3` (2026-07-26 07:33, "fit in flight") contains `run_census.py` with the full band rule including the position −1 choice; the gate verdict was committed at `0e737c4` (14:36), seven hours later. The rule pre-dates any gate or census output. The *scope* criticism stands (prediction-position-only census; content-position variant never run) but the "nullability built in with knowledge of the gate" insinuation does not.

3. **The five untraceable numbers in RESULTS_EXP010B.md.** code-verify right; I confirmed directly against `terminal_characterisation_small010b.json` (values quoted in Finding 1). results-other's otherwise-favourable read of EXP_010B must be amended: verdicts fine, descriptive layer contradicted by its own artifact.

4. **The dual permutation registration.** code-verify right; I confirmed both files, the absence of any cross-reference (grep: zero), the absence of a supersession marker, and — going beyond code-verify — the commit timestamps showing the rewrite preceded execution, which supports "honest re-registration" over "spec-shopping." The A4 verdict is robust to either spec; A1_tail (a set that exists only in the executed spec) is the one threshold-sensitive row.

5. **foundations' worry that H11 and the 010c/010d family were never pre-registered.** Resolved mostly in the project's favour, with one exception. Every spec (010c, 010c2, 010c3, 010c3b, ROBUST, VARIANTS, PERM, PYTHIA, 010b) has a commit-verifiable spec-before-results sequence (checked with `git log --diff-filter=A`). The exception is EXP_010d/H11: spec, harness, and results entered as one commit on the stranded PR #5 branch (verified via GitHub API — the PR has exactly one commit, baf33989). H11's "pre-registered" status is testimonial. foundations' related point about non-unified verdict vocabulary is confirmed and stands.

6. **BOS convention.** code-verify right that the deviation note is suspect: `atr_engine2.py` passes raw strings to `run_with_cache` (lines 142/200/309/342), and transformer_lens's GPT-2 default is `prepend_bos=True`, so the engine most likely *does* prepend BOS, contra `RESULTS_JLENS_MEDIUM.md:293`. Unresolvable definitively without running the pinned TL 3.5.1; either way it is a one-line check that must happen before any lens-on-terminals work.

7. **A4/O8 stopping-rule check and the natural_i ARI arbiter.** results-windows right on both: the stopping-rule variation ran only at I7/I9/X1017 (5-prompt slices); the session note's proposal to run it at A4/O8 has no spec, issue, or results anywhere; and the natural_i ARI test remains unfiled (only the 07-27 session note describes it). Both gaps confirmed by grep across the tree.

8. **H12a "generous" grading.** I side with the record against results-windows here: `EXP_010c3_SPEC.md`'s pre-registered table row 129 ("5→21 and 7→21 both resemble the i≤6 punctuation/fragment character → H12a supported on the onset side") matches the observation exactly; the "7→21 whole-word but 5→21 not" row was a *different* branch that did not obtain, not a failed sub-condition of H12a. results-windows' broader verdict-generosity concern retains force via H9a (unmetricized "most different") but not via H12a.

9. **`--arms` overwrite hole.** code-verify right; confirmed in `run_exp010c.py:352–366` — the variant list checks seed/subset/prompt-offset/n-prompts/renorm but not `args.arms`, and tag-collision with tier names is unchecked.

## LEADERSHIP QUESTION — what is dead, what survives, what next

**Dead (on the artifacts, regardless of formal kill status):**
- The naive 40–90% depth mapping (layers 10–21) that set the window grid's coordinates — no J-lens support anywhere in it.
- H7's existence conjunct at 345M: Medium has no coherent J-lens band at prediction positions. Phase 3's four-model census (EXP_012) as originally motivated — "band presence/shape predicts ATR regime" — is strategically dead: the middle-scale model has no band, and Pythia shows window effects are placebo-generic, so a band/regime correlation has nothing to correlate. Only a content-position census could revive it.
- The "two instruments localise the same band" prediction (07-23) — recorded as not borne out; the census sees nothing at {8,10} vs {9}.
- H11/H11a-for-A4 — refuted, robustly.
- The *workspace reading* of H5: the letter of H5 survives trivially, but there is no ATR-side candidate band on Small (no sub-window carries the landscape), the Medium word cells are isolated islands rather than a band, and window-dependence per se is placebo-generic. H5-as-workspace-evidence is spent.
- EXP_013m as decisive arbiter: with a MARGINAL gate and no band, it is pre-declared ambiguous-on-null — it can rule latent content *in* but never *out*. Every claim currently sheltering behind it (the record defers to it ~15 times) is sheltering behind an instrument that cannot adjudicate negatively. Demote it.
- Also note: the plan-level kill criteria **can never fire as written** — the conjunction requires "EXP_010 shows no qualitative window-dependence," which is already false. The frame can currently be neither killed nor confirmed by its own registered rule; the criteria need re-registration.

**Survives:**
- EXP_011 (Small) / EXP_011m (Medium) — subspace overlap, the one registered test that bypasses readout rankings entirely and the only remaining leg that can formally close or save the workspace framing. Note the hidden costs: the Small lens was never fitted (no RESULTS_PHASE0.md exists anywhere in this repo) and the Medium lens artifact is gitignored, so a refit (~7h CPU) may be needed; the BOS seam must be settled first.
- H8 (supported, loudness-confounded, natural_i mirror pending), H6 (untested), Q-D/EXP-D (unconditional, still unrun — the cheapest fully self-contained deliverable in the whole plan, needing only the frozen Stage 1 zip).
- The A4/O8 islands and A4's permutation pass — as ATR-internal findings about the looping procedure, no longer as workspace evidence.
- The apparatus-mask hypothesis — the programme's new centre of gravity, properly fenced, arbiters named.

**Single highest-information next experiment: file and run the natural_i ARI comparison** (arbiter 3 of the 07-27 synthesis). Compare the *committed* natural-energy A0 terminals (`terminals_energynorm_A0.pt`) against Small's *committed* partition (`terminals_small_010d.pt`, same 25 prompts) using the existing `compare_small_basins.py` ARI + permutation machinery. It is pure analysis — zero model compute, all inputs already in the repo, discriminating outcomes already written down in advance: significant ARI without `D` present → the Small-like partition does not require the loud convention and the mask hypothesis strengthens (which would reframe the programme's founding cross-model contrast as an apparatus artifact); chance ARI → the PR #5 structure is bound to the loud convention and the mask hypothesis deflates. Either branch moves the deepest live question the programme has, and its outcome determines whether the surviving J-space work (EXP_011m) is even aimed at the right phenomenon. It must be *specced first* — it is currently vapor, which is itself a register gap. Runner-up, and the right move immediately after: EXP_011m with the BOS convention resolved and an anisotropy-matched null, promoted explicitly to primary arbiter in place of EXP_013m. (Caveat to carry into the spec: natural_i A0 runs are 0/25 converged, so the test varies energy, mask, and convergence together; the pre-stated follow-up control in the 07-27 note covers this.)

## ACTION PLAN

Ordered; each step assignable to one operator/agent session.

1. **File the natural_i ARI spec and run it** (pure analysis; inputs `terminals_energynorm_A0.pt` + `terminals_small_010d.pt`; adapt `compare_small_basins.py`; pre-registered outcomes copied verbatim from SESSION_2026-07-27). Owner: analysis session. Half a day.
2. **Run the A4/O8 stopping-rule stability check** (check_start 10 vs 100 at the two flagged cells, 25 prompts each; the record's own proposal). This is the cheapest test that could invalidate the flagship cells and it has silently dropped off the queue. Owner: runner session. Hours of CPU.
3. **Register-integrity fixes, one PR:** (a) correct `RESULTS_EXP010B.md:45,52–54` against the committed characterisation JSON with a dated correction note; (b) add "SUPERSEDED by EXP_010c_PERM_SPEC.md (0ca5829); never executed" to `PERM_TEST_EXP010c_SPEC.md` and record the re-registration rationale in RESULTS_EXP010C; (c) dated annotations: stale "single seed" caveat in RESULTS_EXP010D → "single prompt subset," stopping-rule caveat added to EXP_010d's partitions, 07-23 session note annotated with the 12-basin count and H11 refutation pointers; (d) dated addendum re-scoping H9/H10a/H10b as conditional on the layer-0/j-scale convention; (e) a custody note on EXP_010d recording that spec and results share one commit. Owner: register session. One day.
4. **Close the code holes:** add `--arms` to the variant guard; reject `--tag`/`--out-suffix` equal to any tier name or `<tier>_harness`; commit a one-line BOS tokenization check (does the pinned TL 3.5.1 prepend BOS on `run_with_cache(str)`?) and correct or confirm deviation §5.4 accordingly; regenerate `permutation_results.json` with the attestation-writing script. Owner: code session. Half a day.
5. **Re-register the kill criteria.** The current conjunction is unfireable (its first leg is already false). Propose: "the workspace framing ends if EXP_011 (Small) and EXP_011m (Medium) both show basin/window terminals projecting onto J-space no differently than matched nulls, given the already-recorded Medium band null and the absence of a Small candidate band." Pre-register the nulls with the PERM spec's rigour. Owner: Fable/design session, TC sign-off.
6. **Run EXP_011m (Medium) as promoted primary arbiter** — BOS resolved, anisotropy-matched null, Medium lens refit if the gitignored artifact is lost (~7h CPU). Simultaneously demote EXP_013m in every record that names it "the registered arbiter" (a one-paragraph note: it can only rule content in, not out). Owner: runner session. 1–2 days.
7. **Run EXP-D (Q-D, Divine characterisation)** from the frozen Stage 1 zip — unconditional finding, no dependencies, still unrun; and run the cheap content-position census variant (position −2, same pipeline) before any "Medium has no band tout court" claim leaves the repo. Owner: runner session. 1 day combined.
8. **Then decide Phase 3 (EXP_012 four-model census) and EXP_011-Small** on the joint outcome of steps 1 and 6 — if the mask hypothesis strengthens and EXP_011m is null, close the workspace framing per the re-registered criteria and write the negative up (the house rule makes it publishable); the surviving paper is then the apparatus-conditionality result plus the A4/O8 island characterisation, which is a genuinely novel methodological finding about activation-loop instruments.
9. **Process guard:** adopt the merge-hygiene rule the PR #5 near-loss demonstrated (no verdict-bearing result may live only in an unmerged PR past the session that produced it), and require spec-commit and results-commit to be distinct commits for anything labelled pre-registered.