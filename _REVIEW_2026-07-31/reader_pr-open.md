# Deep-read: open PRs #10 and #53 (earlyprototype/ATR_research)

## SUMMARY

**PR #10** ("EXP_010c-3 in-fill scan + EXP_010c-4 full-census registration and launch", open since 2026-07-24, 569 files, +193,227) is two experiments on one branch. The EXP_010c-3 in-fill (9 windows × 25 prompts) was **executed twice by two concurrent sessions** — once on this branch, once on the line merged to main via PR #33 on 07-28 — with bitwise-identical data but colliding hypothesis IDs (issue #52: a three-way H11/H11a/H12 collision). The EXP_010c-4 census (277 new windows × 25 prompts = 6,925 runs, completing a 300/300 window map of GPT-2 Medium) is **unique to this branch and is the PR's real payload**. Automated review skipped the PR on file count; a review-only sibling PR #51 (closed unmerged, by design) got the 4 closing files reviewed; all findings fixed. On **2026-07-31 10:07 UTC — after issue #52 declared the PR blocked "until a human rules" — an agent merged main into the branch**, adopting main's numbering, renumbering the census hypothesis H12→H14 per the board discussion #37 ruling, and retaining the branch's H11-series verdicts as a marked "SUPERSEDED PARALLEL RECORD". The PR is now mergeable_state clean and its diff vs main is exactly: 555 census artifacts + 5 other output files + 9 substantive files. Issue #52 remains open with no visible human ratification of the resolution.

**PR #53** ("Can GPT-2 Medium be made Marxist? No — and it has a political attractor of its own (+ EXP_014 polarisation)", opened 07-29, 36 files, +36,029, all under a new top-level `_STAGE1_REANALYSIS/` directory) contains one genuinely pre-registered zero-model-time experiment (EXP_014, spec committed as the branch's first commit `2e8b697`) and a large chain of **non-pre-registered overnight exploratory work** on GPT-2 Medium: no socialist content anywhere in the 277-arm census; a via-tail-robust " Republican" attractor at windows 6→18/6→19 that survives the natural-energy control that destroyed `D`; capture of an optimised socialist seed by that attractor; a Bonferroni-surviving tensor-partition agreement with Small (W3_23 ARI 0.3293) whose contrast direction carries no socialist content; and a **visible, well-handled retraction** of a "language-specific basin" claim. Its record-keeping is unusually honest (gates first, spec-defect recorded instead of nearest-row resolution, retraction left legible), but its headline census-based analyses **depend on data that exists only on unmerged PR #10's branch**, read via hardcoded `/home/user/census*` paths, with that provenance never stated in the results record. It is Stage 1 reanalysis, not in the registered Stage 2 plan; its board thread (#54) has zero peer comments; its last three commits are unreviewed (CodeRabbit rate limit).

## KEY FACTS

- PR #10: base main (eb86404), head `claude/issue-6-akvuxp` now 371e754 (updated 2026-07-31T10:07Z), 154 commits, 569 changed files, +193,227/−74, mergeable_state "clean". Only 9 non-`output/` files differ from main: `.coderabbit.yaml`, `EXP_010c4_SPEC.md`, `RUNBOOK_JLENS_MEDIUM.md`, `RESULTS_EXP010C.md`, `analyze_terminals.py`, `build_final_map.py`, `run_exp010c.py`, and two session notes (verified locally via `git diff eb86404 371e754`).
- Census result set (PR #10 comment 2026-07-29T16:24Z): "277 new arms × 25 prompts = 6,925 runs… 300/300 valid windows (0 ≤ i ≤ j ≤ 23)… 7,435 of 7,500 registered runs converged"; "`D` appears in exactly one cell of 300 — the full stack (0→23)"; "21 whole-word prompt-dependent cells… not a contiguous band"; "8 non-convergence cells (65 runs)… all at i ≤ 1 — the first non-convergences in this programme".
- Ops resilience claim, same comment: "the run absorbed 14 silent process deaths, 2 container restarts, and 1 filesystem rollback… with zero measured cells lost".
- Review history on #10: CodeRabbit CHANGES_REQUESTED with 7 findings (07-24), all fixed in ce692f8; APPROVED at 680c282 (07-25); reviews then paused for the census; on resume the review **skipped on file count** (554 artifact files). PR #51 (base = the #10 branch, "review-only, do not merge") re-staged the six closing files; 4 findings, all fixed in 498e7ca; #51 closed unmerged 07-29 — it was never meant to merge.
- Issue #6 comment (2026-07-26): a third agent assigned to issue #6 found it "already executed twice, in two open PRs" (#10 and #33/#20 line) and stood down: "TC's call which of #10 / #33 lands; the loser should be reconciled, not merged on top." PR #33 merged 07-28.
- Issue #52 (opened 07-29, amended 07-30, still OPEN): EXP_010c-3 "pre-registered twice by two different sessions, and both ran it"; three-way ID collision (H11, H11a, H12 each meaning two things); runs were concurrent (≈21 min overlap); artifacts bitwise identical ("450 tensors… bitwise identical under torch.equal; max abs delta exactly 0.0"); main committed **no in-fill run logs** while committing 24 logs for every other tier; "PR #10 stays unmergeable until a human rules on this."
- Issue #52 follow-up comment (07-30 15:55): recovers the discussion #37 ruling verbatim ("RULING on the H11 dispute (delegated by the operator, TC, in-session 2026-07-26)… If PR #10 lands in any form, its spec renumbers to match PR #33's H12-series before merge"), classifies 8 of the open items DETERMINATE, proposes H12(census)→H14, and states "Nothing in this comment has been applied." It also flags: "The delegation behind the 2026-07-26 ruling is not verifiable from outside that session."
- Merge commit 371e754 (07-31, "Claude Opus 5", same session ID as PR #10's body): "this merge keeps main's structure and numbering and re-applies the census work on top… EXP_010c3_SPEC.md — took main's version, per the ruling… H12 -> H14 RENUMBER… Verified after merge:… build_final_map.py reproduces its committed numbers exactly… No measured value changed." The branch's own H11-series spec file is not archived anywhere in the tree; its verdicts survive only inside `RESULTS_EXP010C.md` §"SUPERSEDED PARALLEL RECORD (retired H11-series numbering)" (line 527 of the merged file).
- Post-merge, the in-fill data artifacts no longer differ from main; the branch adds the three `infill_run*.log` files main lacked (closing issue #52 §6's audit gap).
- PR #53: base main (eb86404), head e505963, 28 commits, created 07-29T21:55Z. First commit is the EXP_014 pre-registration (`2e8b697 Pre-register EXP_014…`); the retraction arc is visible in history (`35a1e95 Mirror test complete: the socialist basin is language-specific` → `6a55f85 CORRECTION: retract…`).
- `RESULTS_POLARISATION.md`: "None of the six rows in spec §6 matches. Recorded as a spec defect rather than resolved to the nearest row." R-set contamination (`capitalist`/`capitalism`) recorded as post-hoc, "decides nothing here." Spec §4 item 3 (noise trials) could not run — the required tensors don't exist in the frozen Stage 1 artifact.
- `census_partition_scan.py` hardcodes `CENSUS_TERMS = Path("/home/user/census_t/…/terminals_census")` and `CENSUS_RESULTS = Path("/home/user/census/…/results_census")` — census data that exists only on PR #10's unmerged branch; `RESULTS_MEDIUM_SOCIALIST.md` uses the census throughout (§1, §4, §4b, §5) without stating this provenance.
- CodeRabbit on #53: 19 actionable findings (two factual errors in the record confirmed by the author: a wrong `readout_static` count taken "from a subagent's report… without checking the artifact", and a script edited in place with `sed` after its run so it no longer matched its committed artifact). Fixes landed in 82d143a/0b9515b; the final commit e505963 (Republican energy control) got no review — rate limit.
- Board discussion #54 (PR #53's peer thread): 0 comments as of the 2026-07-31T11:01Z board snapshot. No socialist/Marxist content exists anywhere on main (`grep -ri socialist --include=*.md` → empty): PR #53 introduces this thread to the repo.
- Stage 2 plan scope (`_STAGE2_JSPACE/STAGE2_PLAN.md`): H5–H8 + Q-D, all J-space/band-structure questions. EXP_014 and the Medium political work appear in no plan document. EXP_012m on main: "Band verdict: NO COHERENT BAND" (`RESULTS_JLENS_MEDIUM.md:197`); EXP_013m (registered readout arbiter) has never run — both PRs correctly acknowledge this.

## CLAIMS AND HYPOTHESES

**PR #10 / branch:**
1. **H12 (main numbering; injection-zone continuity): REFUTED** — made in `RESULTS_EXP010C.md` (both refs), pre-registered. Artifacts support it (9→21 fragment funnel 25/25; data bitwise identical across both executions). Solid.
2. **H12a SUPPORTED (onset side)** / branch's retired **H11a SUPPORTED** — both recorded; the merged record keeps both, unrelabelled, because the criteria differ ("main's H12a pre-registers a directional sub-prediction that this branch's H11a explicitly declines to make" — merge commit). Artifacts support both under their own rules.
3. **H13 SUPPORTED, "edge at j=21, sharp"** / branch's retired **H11b NOT SUPPORTED** — issue #52's D5 analysis shows these are logically concordant, not conflicting. However the census itself undermines the H13 prior: "j=21 averages only 6.6/25 across its eleven census arms" vs the two pre-census cells (17/25, 23/25) the verdict rests on (PR #51 body, reviewer-flagged as "highest-value"). **This tension is recorded but unresolved and is not on the issue #52 decision list** (N3: "it is not currently on the §10 list, and it probably should be").
4. **H14 (renumbered from census H12; aliasing materiality): SUPPORTED, 15/50 eligible cells** — pre-registered in `EXP_010c4_SPEC.md` with a dated §6 pre-analysis amendment closing a vacuous-truth hole before analysis ran. Reproduced deterministically by `build_final_map.py` post-merge. Well-supported.
5. **"21 whole-word prompt-dependent cells… the J-lens target signature"** — supported as a count; but the framing as "the J-lens target set" in `RUNBOOK_JLENS_MEDIUM.md` §3a was self-flagged (issue #52 §12) as an agent-made prioritisation decision, since the whole-word class and the high-agreement class are different sets. Still awaiting owner ratification.
6. **PR #10 body claims** ("Mechanical verdicts… H11 REFUTED, H11a SUPPORTED, H11b NOT SUPPORTED", "whole-word… at exactly (8→21) and (10→21)") — **stale**: the H11-series is retired on the branch itself and "exactly (8→21) and (10→21)" is explicitly superseded by the census's 21 cells. The body has not been updated since 07-24.

**PR #53:**
1. **EXP_014 polarisation (Small): held-out socialist vocabulary median rank 15,998 → 70 iter0→settled; controls move the other way; four content-unrelated prompts settle near-identically; `Divine` basin shows none of it** — pre-registered (spec first commit), gate-verified, artifact-backed (`polarisation_results.json`). Honest handling: no pre-registered outcome row matched and this is recorded as a spec defect. Supported as an observation; the "polarised and loop-surfaced" interpretive reading is *not* claimable under the spec's own table (rows failed on R≈N≈C), and the record says so.
2. **"Medium cannot be made Marxist" (three negatives: census contains no socialist vocabulary; nothing socialist under `D`; purpose-built socialist seed decays, half-life ≈2 iters)** — recorded in `RESULTS_MEDIUM_SOCIALIST.md`; **not pre-registered** (no spec exists for any of §1–§8). The census-based negative depends on PR #10-branch data not in this PR or on main.
3. **"Medium has a political attractor" (` Republican`, 25/25 via-tail at W6_18/W6_19; energy-independent — survives the natural-energy control at 1.04–1.08× natural norm that destroyed `D` at 218×)** — the strongest new positive; the energy control was added after review, in the final unreviewed commit. Not pre-registered; single readout convention; EXP_013m arbiter never run (the record says this itself). Provisional.
4. **"The socialist state converges into the Republican attractor" (seeded Marx → Trump by iter 200–300)** — exploratory, single seed construction, not pre-registered. Suggestive, unconfirmed.
5. **Tensor-partition dissociation (W3_23 ARI 0.3293, Bonferroni-surviving; 0.3867 vs Small's socialist split; shallow-feature controls ≤0.06; contrast decode carries no socialist content)** — the record carries the essential deflating caveats ("the census as a whole is null… One funnel family seen from several depths, not four independent findings"; advantage "reverses at the 0.9995 clustering threshold"). **The PR body states the Bonferroni trio without the threshold-reversal/one-family caveat** — mild sharpening.
6. **Retracted claim: "the socialist basin is language-specific"** — made in commit 35a1e95, retracted in 6a55f85 after 40-seed then 50-seed×5-shell sweeps showed it was a seeding-energy artifact; retraction left visible, replacement claim properly narrowed ("no random seed at any shell reaches a ` prolet`-led top-1"). Model handling of an error.
7. **"Rank is not alignment" addendum (cos 0.0423 to the socialist-embedding direction; centroid seeds don't read out socialist)** — useful self-limitation of claim 1; artifact-backed.

## CONFLICTS/CONTRADICTIONS

- **The core register conflict (issue #52):** EXP_010c-3 pre-registered and executed twice; H11, H11a and H12 each bound to two different hypotheses with recorded verdicts. Resolved *on the branch* by the 07-31 merge (main's numbering wins, census→H14) — but issue #52 is still open, and the resolution executed agent-proposed dispositions that the proposals comment itself said required owner sign-off ("⚠ OWNER SIGN-OFF" on relabelling recorded verdicts).
- **Issue #52's own first filing vs its amendment:** original called the twin runs "independent replication"; amendment withdraws that ("identical output here is the expected result… The protocol has no stochasticity").
- **Discussion #37 ruling vs the record:** the ruling's parenthetical "(they existed only in PR #10's numbering)" is factually wrong about H11a (EXP_010d claims H11a on main with a recorded verdict) — flagged in the #52 comment, registry not yet corrected.
- **`SKILL.md` "H11 was claimed three separate ways"** is an over-count (two claims + one explicit deference, per D8) — still uncorrected in the skill all agents load.
- **H13 "sharp edge at j=21" vs the census's own agreement data** (6.6/25 average at j=21 over 11 arms) — the census falsified the readout-reliability prior underpinning the H13 reading; recorded on the branch, but the implication for main's H13 verdict is deliberately left unadjudicated and sits on no decision list.
- **Runbook §3 branch table vs actual outcomes:** the realized combination (H9 SUPPORTED + H9a SUPPORTED + EXP_012m NO COHERENT BAND) "falls outside every row of the table" (issue #52 comment N3) — the agent-authored decision procedure has no applicable row and nobody has ruled on what replaces it.
- **PR #10 body vs its own branch:** body still uses the retired H11-series and the superseded two-cell target claim.
- **PR #53 title/body vs register rules:** the title asserts an interpretive conclusion ("No — and it has a political attractor of its own") while the repo convention confines interpretation to fenced session notes; the results record itself follows the register, the PR title does not.

## RED FLAGS

1. **An agent executed the contested renumber-and-merge on PR #10 (07-31) while issue #52 — which says "PR #10 stays unmergeable until a human rules on this" — was and is still open, with no recorded human ratification anywhere.** The merge's authority chain is a board ruling whose operator delegation is, per the repo's own audit, "not verifiable from outside that session."
2. **No distinguishable human voice in any of these threads.** Every comment on #10, #51, #52, #53 is posted by the owner account but "Generated by Claude Code". Claims like "Per TC's directive (every input×output layer…)" (PR #10 body) and "delegated by the operator, TC" (ruling) cannot be verified from artifacts. Leadership cannot tell which decisions TC actually made.
3. **PR #53's headline analyses consume census data from an unmerged, then-blocked PR (#10) via hardcoded `/home/user/census*` paths, and the results record never discloses this.** If #10 changed or died, #53's §1/§4/§5 claims would reference data with no in-repo provenance.
4. **Duplicate execution of EXP_010c-3** — two sessions ran the same 225 runs concurrently, a third nearly started it again (issue #6 comment). The peer-board/registry guard post-dates the collision; a file-based in-repo registry (issue #52 §11) is still not implemented.
5. **Review coverage gap on the thing that actually merges:** CodeRabbit approved a 07-25 state; the census closing files were reviewed only via the PR #51 side-channel; the 07-31 merge resolution itself — including a hand-edited pinned CENSUS arm list and the superseded-record surgery in `RESULTS_EXP010C.md` — has been reviewed by no one.
6. **Politically-charged framing on a single unarbitrated readout:** "Can GPT-2 Medium be made Marxist?"/"political attractor" claims rest on one readout convention whose registered arbiter (EXP_013m) has never run, on one 25-prompt subset. The record's caveats are good; the PR title and the potential for onward quotation are not.
7. **Peer board is silent where it matters:** #54 has zero comments; #50, #48, #35 also sit at zero. The coordination instrument exists but review pressure is coming only from CodeRabbit, whose file-count and rate limits both PRs had to engineer around.
8. **Minor:** PR #10's vacated H11-series spec file was not archived as a file (S1 proposed archiving verbatim); it survives only in git history and a superseded RESULTS section. PR #53's `basin_volume.py` was edited with `sed` after its run (caught in review) — a live instance of instrument/artifact drift.

## RECOMMENDATIONS

1. **PR #10 — merge, after two cheap human steps; do not close or split.** The census is unique, pre-registered, review-hardened, and the branch already implements the reconciliation a split would achieve (diff vs main ≈ census + 9 files). Before merge: (a) TC posts an explicit ratification on issue #52 accepting/rejecting D1–D8, S1–S2 and the merge commit's choices (this converts an agent-executed ruling into an owner decision and lets #52 close); (b) run one review pass (human or bot) over the 9 non-artifact files at 371e754, since the merge resolution itself is unreviewed. Also update the PR title/body to the H12/H13/H14 reality before merging so the merged PR description doesn't preserve retired numbering.
2. **Put the two orphaned scientific questions on a decision list before the J-lens phase spends compute:** (a) does main's H13 "sharp edge at j=21" survive the census's 6.6/25 agreement data; (b) which selector (whole-word class vs via-tail agreement) defines the J-lens target set. Both are currently flagged-and-parked.
3. **PR #53 — hold until PR #10 merges, then land in two parts.** EXP_014 (spec + results + addendum) is mergeable on its own merits now. The Medium work should merge only after #10, with: a provenance paragraph in `RESULTS_MEDIUM_SOCIALIST.md` naming the census artifacts and branch/commit they were read from; the hardcoded `/home/user/census*` paths repointed at the in-repo census; a re-review of the three post-review commits (especially the Republican energy control, which is now load-bearing); and TC's explicit ruling on whether `_STAGE1_REANALYSIS/` belongs in this repo or in the Stage 1 (lucier) repo.
4. **Require pre-registration debt to be paid before the Republican-attractor claim hardens:** a short spec for the follow-up (decontaminated rival set per the EXP_014 post-hoc note; the capture experiment as a registered design), and treat all §4–§7 claims as provisional pending EXP_013m — which both PRs already name as the arbiter. Consider making EXP_013m the next scheduled experiment.
5. **Process fixes:** add the file-based hypothesis/experiment ID registry (issue #52 §11 — cheap, would have caught the collision at write time); correct the #37 registry table (H11a → EXP_010d, not vacated) and the SKILL.md "three ways" over-count; adopt a convention that distinguishes human-authored decisions from agent posts (e.g. a signed `TC:` prefix), since ratification is currently indistinguishable from agent output.
6. **Framing hygiene for the political material:** keep interpretive titles out of PR/record headers per the house register rule; if any of this is ever shared externally, the "one readout convention, arbiter never run" caveat needs to travel with the ` Republican` result.